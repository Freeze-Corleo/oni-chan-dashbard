import HomeRoot from '../../components/organisms/HomeRoot';
import RegisterContent from '../content/RegisterContent';

const Register = () => {
  return (
    <HomeRoot children={<RegisterContent />} />
  );
};

export default Register;
